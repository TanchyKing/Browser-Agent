# 候选 D · LLM 评估与"裁判可靠性"研究 — 精简分步计划

> 新技能：**评估工程 / LLM-as-judge 校准 / 评判者元评估**（区别于 p1 的 RAG、p2 的微调）。
> 推荐度：储备。统计味最重、技能稀缺；但须把立意收紧到"评判者本身可靠吗"，避免与 p1 的 RAGAS 撞车。

## 一句话定位
不是"用 LLM 评别人"，而是**质疑评判者本身**：构建一个金融问答的评估场景，系统量化 **LLM-as-judge 与人工标注的一致性、位置/长度/自我偏好等偏置、以及打分校准**，回答"自动评估到底能不能信、在什么条件下信"。

## 基于的开源底座（可扩展点）
- **confident-ai/DeepEval**（G-Eval：带 CoT 的 LLM-as-judge）、**Langfuse**（judge + 人工标注 + 测试集）。
- 参考基准：JudgeBench（评判者的评判）。
- 扩展点：① 中文金融场景的 judge 可靠性评测（开源多为英文通用）；② 偏置注入实验（位置/长度/风格）；③ judge-人一致性的统计层（κ、校准曲线），这是多数框架不提供的。

## 用到的新技能（简历可写）
评估工程、LLM-as-judge 设计与校准、评判者偏置量化、标注一致性统计（Cohen's κ / 相关）、评估可靠性方法学。

## 机器可行性
评估为主，GPU 压力小：本地 qwen3:8b 当 judge（复用 p1）、或多个本地小模型对照。人工标注由你完成（统计背景正好把关）。很轻量。

## 技术栈
Python · 本地 LLM(Ollama) 作 judge · DeepEval / Langfuse 或自写评估循环 · pandas · scipy/statsmodels(κ、bootstrap、校准、显著性) · matplotlib(校准曲线)

## 分步计划

### Step 1 — 评测场景与"被评对象"
- 选一个明确任务：金融问答答案质量打分（可直接复用 p1 的问答输出作为被评样本）。
- 准备 60-120 条"问题 + 候选答案"，覆盖好/中/差/有事实错误/拒答等质量档次。

### Step 2 — 人工 ground truth（你的统计背景在这里立威）
- 你对每条候选答案人工打分/排序，作为金标准；记录评分标准（rubric）。
- 若有条件做二次标注或自一致性检查，更能讲"标注可靠性"。

### Step 3 — LLM-as-judge 基线
- 用本地 qwen3:8b（+ 可选第二个模型）按同一 rubric 打分/排序（pointwise 与 pairwise 两种范式）。
- 实现 G-Eval 风格的带 CoT 评判，作为主 judge。

### Step 4 — 一致性与校准（核心，统计味全在这）
- judge vs 人：分类用 **Cohen's κ**、连续分用相关/一致性、排序用 Kendall τ。
- **校准**：judge 打分的可靠性曲线，看高分是否真对应高人评分。
- 多个 judge 之间一致性，识别"模型间系统性分歧"。

### Step 5 — 偏置注入实验（项目的记忆点）
- **位置偏置**：pairwise 时交换 A/B 顺序，看结论是否翻转。
- **长度/冗长偏置**：同质量但更长的答案是否被高估。
- **自我偏好**：judge 是否偏爱与自己同源模型的输出。
- 每种偏置给出量化幅度 + 显著性（配对检验 + Holm 校正，平移 p2 经验）。

### Step 6 — 收尾
- 结论表：在什么条件下 LLM-judge 可信（κ 阈值）、哪些偏置显著、缓解手段（顺序随机化、长度归一）效果。
- README + 简历 bullet。

### Stretch
judge 集成/投票是否提升一致性；用偏置缓解后的 judge 反过来改进 p1 的 RAGAS 评估流程。

## 实验记录门禁
沿用 p1/p2：每个改动先写 `实验记录.md`（judge 模型版本、rubric、范式、命令、κ/偏置幅度/显著性）；**日志未更新不进下一步**；judge 结论必须有人工金标准支撑，禁止"自动分数自证"。

## 与 p1 的边界（必须守住，否则像 p1 延伸）
- p1：**用** RAGAS 评 RAG 系统。
- D：**质疑评判者**——RAGAS/LLM-judge 这类自动评估自己可不可信、偏在哪、何时该用人工兜底。立意是"元评估"，不是再做一遍 RAG 评测。

## 面试脊柱（三必答）
1. **为什么不能无脑信 LLM-as-judge？** 它有位置/长度/自我偏好等系统性偏置；我用偏置注入 + 与人工的 κ 一致性量化了"何时可信、偏多少"。
2. **怎么衡量 judge 可靠？** 不是看 judge 自己的分，而是 judge-人一致性（κ/τ）+ 校准曲线 + 偏置幅度的显著性检验。
3. **这和你的 RAG 项目什么关系？** RAG 项目里我用了 RAGAS；这个项目是回头质疑那套自动评估本身可靠吗——把评估从"工具使用者"升级成"方法学审视者"。
