# 候选 B · 金融时序预测基础模型 — 精简分步计划

> 新技能：**时序基础模型 + 概率预测 + 严谨回测**（区别于 p1 的 RAG、p2 的微调）。
> 推荐度：首选。8GB 下零样本即可出可信结果，最契合统计背景。按步骤推进，不限定天数。

## 一句话定位
拿一批真实金融时间序列（A 股行情 / 宏观 / 财报指标），用**时序基础模型零样本预测**，并用**概率指标 + 统计检验**严谨地比较基础模型 vs 经典基线（Naive / ARIMA / ETS），量化"基础模型到底赢在哪、赢多少、是否显著"。

## 基于的开源底座（可扩展点）
- **Amazon Chronos-2**（`amazon-science/chronos-forecasting`）：成熟、概率预测、支持协变量；小档可 8GB 跑。
- **Google TimesFM 2.5**（≈200M，长上下文 16k，支持 LoRA 微调与 XReg 协变量）。
- 备选：Lag-Llama（概率分布预测）、Salesforce MOIRAI-2。
- 扩展点：① 中文 A 股 + 财报指标的零样本评测基准；② 协变量（成交量/行业指数）注入；③ 概率校准与回测层（多数开源 demo 只给点预测、不做校准与显著性）。

## 用到的新技能（简历可写）
零样本/少样本时序预测、概率预测与不确定性量化、预测回测（滚动原点 backtest）、统计显著性（Diebold-Mariano）、校准评估（覆盖率/分位损失）。

## 机器可行性
TimesFM 2.5≈200M、Chronos 小档均可在 8GB 显存零样本推理；ARIMA/ETS 走 CPU（`statsforecast`）。32GB 内存批量回测充足。**主线零样本不训练**，最快出结果；LoRA 微调仅作 stretch（且不与 p2 撞，因为这里微调的是时序模型不是 LLM）。

## 技术栈
Python · pandas · AkShare（A 股行情/财务） · Chronos / TimesFM · statsforecast(ARIMA/ETS/Naive) · numpy/scipy(DM 检验、覆盖率) · matplotlib · 评估自写 · 可选 PyTorch(LoRA stretch)

## 分步计划

### Step 1 — 数据与切分（防前视偏差是命门）
- 用 AkShare 拉 15-30 支标的的日频行情 + 若干宏观/财报指标，统一成 `(series_id, timestamp, target, [covariates])` 长表。
- 固定**滚动原点回测**协议：训练窗 → 预测 horizon（如 5/20 日）→ 向前滚动；严格只用 t 时刻前的信息。
- **坑**：复权、停牌缺失、未来信息泄漏（用了当日收盘去预测当日）。先把数据协议写死并单测。

### Step 2 — 经典基线（先有 before 数字）
- `statsforecast` 跑 Naive / SeasonalNaive / ARIMA / ETS，给出点预测 + 预测区间。
- 这是必须的对照：基础模型的价值要相对基线说，不能只报绝对误差。

### Step 3 — 基础模型零样本接入
- 接 Chronos-2（或 TimesFM 2.5），同一回测协议下做**零样本**点预测 + 概率预测。
- 显存：确认 8GB 下批量推理不 OOM；必要时减 batch / 用小档。

### Step 4 — 评估（点 + 概率 + 校准，统计味在这里）
- 点误差：MASE / sMAPE（跨标的可比，别只用 RMSE）。
- 概率：分位损失（pinball）/ CRPS；**校准**：预测区间经验覆盖率 vs 名义覆盖率。
- 显著性：对 base vs 基线的逐序列误差做 **Diebold-Mariano 检验**；多标的是一族比较 → 用 **Holm 校正**（直接平移 p2 的多重比较经验）。
- 报均值 ± 标准差，不报单次最优。

### Step 5 — 协变量 / 误差分层（拉开深度）
- 给基础模型注入协变量（成交量、行业指数）看是否提升，作受控对照。
- 误差分层：高/低波动期、趋势 vs 震荡、不同 horizon 分别看，解释"基础模型在什么 regime 赢"。

### Step 6 — 收尾与演示
- 一张回测图（预测带 + 真实）+ 一张对比表（各模型 × MASE/CRPS/覆盖率/DM 显著性）。
- README + 简历 bullet；可选 Streamlit 小面板复用 p1 经验。

### Stretch（有余力）
TimesFM LoRA 微调对照零样本；加入 risk-aware 指标（方向命中率、回撤）；多变量联合预测。

## 实验记录门禁
沿用 p1/p2：每个可验证改动先写 `实验记录.md`（日期、Step、改动、固定回测协议与随机种子、命令、指标、显著性），**日志未更新不进下一步**；负结果（基础模型未显著赢基线）如实记录——这本身是有价值的结论。

## 面试脊柱（三必答）
1. **零样本时序基础模型为什么可能赢经典模型？** 大规模预训练学到跨域时序模式，对短历史/冷启动尤其有利——但要用 DM 检验证明是否显著，而非看平均误差。
2. **点预测对了不等于预测对了？** 必须看概率校准：区间覆盖率偏离名义值说明不确定性估计有偏，金融风控里这比点误差更重要。
3. **怎么防前视偏差？** 滚动原点回测 + 严格信息截断 + 单测，这是时序项目可信度的地基。
