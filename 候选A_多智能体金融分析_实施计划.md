# 候选 A · 多智能体金融分析 + 严谨回测 — 精简分步计划

> 新技能：**Agent 编排 / 工具规划 / 多智能体协作**（区别于 p1 的 RAG、p2 的微调）。
> 推荐度：次选。最有 2026 热点叙事；用统计严谨回测兜住 agent "看着会跑"的硬伤。

## 一句话定位
搭一个**拟人化金融分析多智能体系统**（分析师 / 风控 / 决策者分工，本地 LLM 驱动），让它对一批标的产出可解释的买卖/持有建议，并用**统计严谨的回测**（去偏 Sharpe、Bootstrap 置信区间、多重比较校正）证明决策"是否真的优于基线、是否显著"。

## 基于的开源底座（可扩展点）
- **TauricResearch/TradingAgents**：拟人化交易公司多智能体框架（分析师/风控/基金经理），高星、易讲故事。
- **Open-Finance-Lab/AgenticTrading**：可追溯实验、回测/纸上交易 playground。
- 扩展点：① 把生成模型从云 API 换成**本地 qwen3:8b（复用 p1 的 Ollama）**；② 接 p1 的金融 RAG 作为一个"研报检索工具"；③ 把简陋回测升级成**统计严谨回测层**（多数开源框架最弱的一环）。

## 用到的新技能（简历可写）
多智能体编排（角色分工、消息传递）、工具/函数调用规划、Agent 决策的可解释日志、Agent 系统评估、统计严谨的策略回测。

## 机器可行性
Agent 主要是 LLM 调用编排，GPU 压力在生成端：本地 qwen3:8b（8GB 已验证，p1 的部署经验直接复用），或云 API 兜底长链推理。回测/数据在 CPU。可行。

## 技术栈
Python · 本地 LLM(Ollama qwen3:8b) 或 API · 多智能体框架(TradingAgents / 自写轻量编排 / LangGraph) · MCP 或函数调用做工具接口 · AkShare 行情 · 回测(vectorbt / backtrader 或自写) · numpy/scipy(Bootstrap、去偏 Sharpe、Holm) · 可选接 p1 RAG

## 分步计划

### Step 1 — 跑通底座 + 换本地模型
- clone TradingAgents（或搭最小多智能体编排），先用其默认配置跑通一个标的的端到端决策。
- 把生成后端切到本地 qwen3:8b（复用 p1 Ollama / provider adapter 思路），确认 8GB 下能稳定多轮调用。
- **坑**：agent 链路长、token 多，本地小模型可能跟不上复杂 prompt；先固定少量角色与短上下文跑通。

### Step 2 — 定义角色、工具与决策协议
- 明确 3-4 个 agent 角色（如基本面分析师 / 技术面分析师 / 风控 / 决策者）与各自工具（行情查询、指标计算、研报检索）。
- 工具用函数调用或 **MCP server** 暴露（可复用 p2 的工具 schema 设计经验）；信息不足要能"暂不决策/要更多信息"（呼应 p2 的 refuse/ask）。
- 决策输出结构化：`{标的, 动作, 仓位, 理由, 引用}`，可程序校验。

### Step 3 — 接入金融知识工具（与 p1 串线，加分不抢戏）
- 把 p1 的 RAG 问答封装成"研报/公告检索工具"，让分析师 agent 调用 → 决策有据可溯。
- 这样 p1+p3 串成一条线："我把上一个 RAG 项目做成了 agent 的一个工具"。

### Step 4 — 严谨回测（项目可信度的命门）
- 固定回测协议：滚动调仓、交易成本、严格信息截断（防前视偏差）。
- 指标：累计收益、**去偏 Sharpe（Deflated Sharpe Ratio）**、最大回撤、胜率。
- 对照基线：买入持有、等权、随机择时。
- 显著性：策略 vs 基线收益做 **Bootstrap 置信区间**；多标的/多策略是一族比较 → **Holm 校正**控制族错误率（直接平移 p2 经验）。
- **坑**：agent 项目最大的坑是"少数几次幸运决策被当成有效"——必须用去偏 Sharpe + 多重比较校正堵住数据窥探。

### Step 5 — Agent 行为评估与错误分析
- 不只看收益：评估决策一致性（同输入多次跑的稳定性）、工具调用正确率、理由与动作是否自洽。
- 错误分层：幻觉数字 / 工具误用 / 理由与决策矛盾 / 过度交易。

### Step 6 — 收尾与演示
- 决策时间线 + 推理日志可视化（agent 项目的卖点就是可解释）。
- 对比表（策略 × 收益/去偏Sharpe/回撤/显著性）+ README + 简历话术。

### Stretch
A2A 多智能体协商；引入 reflection/辩论机制对照单 agent；纸上交易实时跑一段。

## 实验记录门禁
沿用 p1/p2：每个可验证改动先写 `实验记录.md`（固定回测协议、随机种子、交易成本、命令、指标、显著性）；**日志未更新不进下一步**；agent 决策必须真跑过回测才算数，禁止把"demo 能跑"当成"策略有效"。

## 面试脊柱（三必答）
1. **多智能体比单 LLM 强在哪？** 角色分工 + 工具使用 + 可解释决策链；但必须用回测证明决策质量，否则只是更复杂的随机。
2. **怎么避免 agent 回测自欺？** 严格信息截断防前视、交易成本、去偏 Sharpe + 多重比较校正防数据窥探——这正是统计背景的价值。
3. **8GB 本地怎么撑多智能体？** 短上下文 + 角色裁剪 + 复用 p1 的显存调度（keep_alive=0、串行），必要时 API 兜底长链。
