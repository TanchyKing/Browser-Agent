# Runtime dependencies for the local Browser Agent project.
# Install only after the environment decision is confirmed:
#   python -m pip install -r requirements.txt

playwright>=1.45,<2.0
pydantic>=2.7,<3.0
PyYAML>=6.0,<7.0
requests>=2.32,<3.0

# Development and verification.
pytest>=8.2,<9.0
