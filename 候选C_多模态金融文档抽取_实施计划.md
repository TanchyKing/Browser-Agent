# 候选 C · 多模态金融文档结构化抽取 — 精简分步计划

> 新技能：**视觉语言模型(VLM) / 版面理解 / 表格与图表抽取**（区别于 p1 的文本 RAG、p2 的微调）。
> 推荐度：储备。是 p1 的自然补全（p1 只做文本，C 做表格/图/扫描件），技术新颖度高。

## 一句话定位
用**多模态视觉语言模型**把金融 PDF（财报表格、研报图表、扫描件公告）抽成**结构化数据**（JSON/Markdown 表），并用一套带 ground truth 的评测量化"抽得全不全、对不对"，重点攻 p1 里被绕过的表格/版面难题。

## 基于的开源底座（可扩展点）
- **PaddleOCR-VL（0.9B）**：超轻量多语种文档解析，8GB 毫无压力，适合做主力。
- **Qwen3-VL-8B**（Q4≈6GB）/ **MiniCPM-V 4.5**：更强的图表/截图理解，做质量候选。
- 扩展点：① 中文财报**表格**结构化（合并单元格、跨页表）；② 图表(柱/折线)数值反提取；③ 抽取结果接回 p1 的 RAG，让数字类问题可答（p1 的 TODO 之一）。

## 用到的新技能（简历可写）
视觉语言模型应用、文档版面分析、表格结构识别、图表数值抽取、多模态抽取评估（字段级准确率 / 表格 TEDS）。

## 机器可行性
PaddleOCR-VL 0.9B 极轻；Qwen3-VL-8B Q4≈6GB 可在 8GB 跑（注意图像 token 多、显存峰值，需控分辨率/页数）。复用 p1 的 PDF 语料，零额外数据采集成本。

## 技术栈
Python · PyMuPDF/pdf2image(渲染页) · PaddleOCR-VL / Qwen3-VL(via Ollama 或 transformers) · 表格评测(TEDS) · 字段级评测自写 · 可选接 p1 Chroma 入库

## 分步计划

### Step 1 — 评测集（先有 ground truth，否则无法说准）
- 从 p1 已有 PDF 里选 20-40 页**有代表性的表格/图表页**，人工标注标准结构化结果（表格→Markdown/JSON，关键财务数字）。
- 题型覆盖：规整表、合并单元格表、跨页表、柱/折线图、扫描件。
- **坑**：标注成本是本项目最大成本，先小而准，别贪多。

### Step 2 — Baseline：纯文本抽取 vs VLM
- 先用 p1 的 PyMuPDF+pdfplumber 文本路线抽同样的页（这就是 before）。
- 再用 PaddleOCR-VL 做版面+表格抽取，对比"文本路线打乱的表格" vs "VLM 还原的结构"。

### Step 3 — VLM 抽取管线
- 渲染页为图 → VLM 输出结构化（表格 Markdown / 字段 JSON）→ 解析校验。
- 强档对照：Qwen3-VL-8B 处理图表数值反提取；注意显存峰值与分辨率权衡。

### Step 4 — 评估（分层、量化）
- 表格：**TEDS**（树编辑距离相似度）衡量结构+内容。
- 字段：关键财务数字的**字段级准确率 / 召回**（净利润、营收等）。
- 图表：抽取数值与真值的误差。
- 分层报告：规整表 vs 合并表 vs 跨页 vs 扫描 vs 图表，说明 VLM 在哪类赢文本路线、赢多少。

### Step 5 — 与 p1 串线（加分）
- 把抽取出的结构化数字入 p1 的库，让 p1 之前答不了的"净利润/同比"数字题变得可答——直接对应 p1 的 TODO。

### Step 6 — 收尾
- 对比表（文本路线 vs VLM × TEDS/字段准确率，按页型分层）+ 样例可视化 + README + 简历 bullet。

### Stretch
对最难的表格类型做 VLM LoRA 微调（会用到 p2 技能，但应用在多模态，叙事不撞）；端到端"PDF→结构化→可查询"小服务。

## 实验记录门禁
沿用 p1/p2：每个改动先写 `实验记录.md`（页型分布、模型版本、分辨率、命令、TEDS/字段指标、显存峰值）；**日志未更新不进下一步**；抽取结果必须对标注集真实评过才算数。

## 面试脊柱（三必答）
1. **为什么金融文档要上 VLM？** 表格/图表/扫描件里的数字是文本抽取的盲区，"垃圾进垃圾出"，VLM 直接看版面能还原结构——这正是我上一个 RAG 项目绕过的难题。
2. **怎么证明 VLM 真的更好？** 用 TEDS + 字段级准确率按页型分层对比文本基线，给出"在哪类页型赢多少"，而不是泛泛说更强。
3. **8GB 怎么跑 VLM？** 0.9B PaddleOCR-VL 做主力、8B 做质量候选，控分辨率/页数压住图像 token 的显存峰值。
