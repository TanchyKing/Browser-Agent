> **旧版归档 / 已被取代（2026-06-22）**
>
> 本文件是早期选题阶段生成的旧版落地计划，保留作决策证据链，不再作为当前执行 source of truth。
> 当前真实计划、进度看板和并行 conversation 边界以
> `本地浏览器智能体MCP工作流_逐步实施计划.md` 为准；实验证据以 `实验记录.md` 为准。
> 后续 agent 不应依据本文件的 0.2 未开始看板判断当前进度。

# 本地 Browser Agent / MCP 工作流自动化项目 · 落地计划

> 目标：在 8GB 消费级 GPU + 纯本地、低数据依赖的约束下，完成一个可运行、可评估、可审计、有安全边界的**浏览器智能体执行系统**。核心不是"做一个会聊天的 agent"，而是做一个**能操作网页、调用工具、记录轨迹、失败可恢复、边界可控、可被基准化评测**的执行系统。
>
> 选题依据见 `项目备选方案.md`：在 p1（金融 RAG）、p2（小模型工具调用可靠性）之外补一个新能力格。简历已含量化交易实习与 p1/p2，故 p3 **不做金融预测/交易方向**，避免重复证明同一类能力；E 填补"agent 执行系统 / 工程编排"这一空白。详细技术分步见 `候选E_本地浏览器智能体MCP工作流_实施计划.md`。

## 0. 角色标记

- **[AI 可做]**：可直接在工作区完成，包括写代码、搭脚手架、自造任务页、实现动作接口、评估与统计、dashboard、README 等。
- **[用户-only]**：只能由你完成或决定，包括关闭占显存程序、确认任务语义与安全策略、决定是否接入任何真实网站、最终简历措辞。
- **[共同]**：AI 先给草案或默认实现，你审阅、确认或补充。

## 0.1 进度与实验记录规则（所有并行对话都必须遵守）

1. **先读计划，再动手**：每次开始前先读本文件与 `候选E_...实施计划.md`，确认自己负责的边界。
2. **先查状态，再执行**：开始某 step 前先看"0.2 进度看板"和 `实验记录.md`，避免重复劳动。
3. **做完一步，先记录，再进入下一步**：每完成一个可验证 step，先更新 `实验记录.md`，再继续。
4. **计划里标注状态**：完成后在"0.2 进度看板"把状态改为 `已完成` 并写一句验证结果。
5. **实验记录必须可复现**：至少写清时间、对话、step、修改文件、运行命令、任务数、模型、结果指标、失败/风险、下一步。
6. **阻塞也要记录**：缺依赖、缺底座、GPU 被占用、需用户确认安全策略时，写 `阻塞` 和需谁处理。
7. **不把未真实运行的结果写成已完成**：成功率、安全测试、local-vs-API 对照必须真跑过才能标 `已完成`；只跑通本地 mock 页，不得写成"真实网站可用"。

`实验记录.md` 是本项目的必需产物，不是可选文档。README、简历 bullet、面试话术最终都从这里提取真实信息。

## 0.1.1 已冻结项目决策

1. **方向**：浏览器/工作流自动化执行系统；**不做金融预测/交易**（简历已有量化实习与 p1/p2）。
2. **任务域**：通用办公/数据流转任务（表单填写、列表筛选排序、跨页面信息搬运、CSV 下载、后台 CRUD 查询），**刻意避开金融语料**，避免与 p1/p2 撞车。
3. **数据策略**：Step 1 起全部用**自造离线任务页**（本地 HTML / Streamlit / FastAPI），不接真实网站；真实网站仅作 stretch 且需 [用户-only] 明确同意。
4. **头条指标取舍**：项目卖点是 **评测框架 + 安全边界 + 失败恢复 + 可审计轨迹**，不是裸任务成功率；本地小模型成功率即使不高也如实报告。
5. **模型对照**：本地 `qwen3:8b`（复用 p1 Ollama）为主线被测对象，API 强模型为**对照基线**；二者差异用统计检验量化（见 E.5）。
6. **安全边界是一等公民**：动作白名单 + 高风险动作拒绝/人工确认 + prompt injection 测试，从 Phase 1 就预留接口，不留到最后补。
7. **统计严谨层（推荐但可选）**：成功率报多次运行均值 ± 标准差或 bootstrap CI；local vs API 用配对 McNemar（平移 p2 经验）。作为差异化加分，不喧宾夺主。

## 0.2 进度看板

状态只用：`未开始`、`进行中`、`已完成`、`阻塞`、`需用户确认`。

| 工作流 | Step | 任务 | 状态 | 最新验证/备注 |
|---|---|---|---|---|
| A | A.1 | 项目目录结构 + 复用 `.venv` | 未开始 | |
| A | A.2 | 依赖文件（playwright/ollama/fastapi/pytest） | 未开始 | |
| A | A.3 | Playwright 浏览器安装与 smoke | 未开始 | |
| A | A.4 | 自造 3-5 个最小任务页 + ground truth | 未开始 | |
| B | B.1 | 统一动作接口（observe/click/type/select/extract/download/finish） | 未开始 | |
| B | B.2 | 选定底座（Playwright MCP 或 browser-use）并接通 | 未开始 | |
| B | B.3 | Trace log（JSONL/SQLite）：页面摘要+候选动作+实际动作+结果+耗时 | 未开始 | |
| C | C.1 | Agent 执行闭环（观察→计划→动作→校验→记录→判完成） | 未开始 | |
| C | C.2 | 动作 JSON schema + 程序侧合法性校验 | 未开始 | |
| C | C.3 | 模型 provider：本地 qwen3:8b 与 API 可切换 | 未开始 | |
| D | D.1 | 安全规则与动作白名单 | 未开始 | |
| D | D.2 | Prompt injection 测试集（恶意网页文本） | 未开始 | |
| D | D.3 | 失败恢复（重试/回退/请求人工确认） | 未开始 | |
| E | E.1 | 评估指标（成功率/步骤数/耗时/无效动作率/恢复率/安全违规率） | 未开始 | |
| E | E.2 | 错误分层（7 类）与逐任务 trace replay | 未开始 | |
| E | E.3 | local vs API 对照 + 统计显著性（McNemar/bootstrap CI） | 未开始 | |
| E | E.4 | Streamlit/HTML dashboard + README + 简历 bullet | 未开始 | |

## 1. 项目前置条件

### 1.1 硬件与系统

| 前置项 | 状态/要求 | 负责人 |
|---|---|---|
| NVIDIA GPU | RTX 5070 Laptop，约 8GB 显存 | [用户-only] 训练/推理前关闭占显存程序 |
| 内存/磁盘 | 32GB 内存；预留 10-20GB（浏览器内核、模型、trace） | [用户-only] |
| 系统/Python | Windows 11 + Python 3.11，复用 p1/p2 的 `.venv`（torch cu128） | [AI 可做] |
| 本地 LLM | 已装 Ollama + `qwen3:8b`（复用 p1） | [用户-only] 保持可用 |

### 1.2 运行环境

| 前置项 | 说明 | 负责人 |
|---|---|---|
| Playwright | `pip install playwright` + `playwright install chromium` | [AI 可做] |
| 浏览器底座 | Playwright MCP 或 browser-use 二选一（Step B.2 定） | [AI 可做] 草案，[共同] 确认 |
| 任务环境 | 本地 HTML 静态页 或 Streamlit/FastAPI mock 后台 | [AI 可做] |
| API fallback | 可选：一个强模型 API key 作对照基线 | [用户-only] 提供 key |

### 1.3 底座选择（Step B.2 冻结）

| 选项 | 适用 | 取舍 |
|---|---|---|
| **Playwright MCP** | 想突出 MCP 工具生态、结构化页面快照 | 简历叙事更"MCP"，与热点契合 |
| **browser-use** | 想最快跑通 agent 闭环、生态成熟 | 上手快，但要主动强调可审计/安全差异化 |
| 自写薄封装 + Playwright | 完全掌控动作接口与 trace | 工程量大但最能讲清内部机制 |

## 2. 并行工作流总览

| 工作流 | 可并行 | 主要产物 | 负责人 |
|---|---:|---|---|
| A. 任务环境与脚手架 | 是 | 目录结构、依赖、自造任务页、ground truth | [AI 可做] |
| B. 浏览器工具层 | 是 | 动作接口、底座接通、trace log | [AI 可做] |
| C. Agent 执行闭环与模型 | 依赖 A/B 最小版 | 执行循环、动作 schema 校验、provider 切换 | [AI 可做] |
| D. 安全边界与失败恢复 | 依赖 C 最小版 | 白名单、injection 测试集、recovery | [共同] |
| E. 评估、统计与交付 | 依赖 C/D 输出 | 指标表、错误分层、统计对照、dashboard、README | [AI 可做] |

## 3. 详细步骤与依赖

### Phase 0：启动准备

| Step | 任务 | 依赖 | 负责人 |
|---|---|---|---|
| 0.1 | 建项目结构、复用 `.venv` | 无 | [AI 可做] |
| 0.2 | 装 Playwright + chromium，smoke 打开本地页 | 0.1 | [AI 可做] |
| 0.3 | 确认 Ollama qwen3:8b 可调用 | 无 | [用户-only] 保活 |
| 0.4 | 选定浏览器底座（B.2） | 0.2 | [共同] |
| 0.5 | 冻结安全策略草案（哪些动作禁止/需确认） | 无 | [共同] |

### Phase 1：最小可运行闭环

| Step | 任务 | 依赖 | 负责人 |
|---|---|---|---|
| 1.1 | 自造 3-5 个任务页 + 每页 ground truth/禁止动作 | 0.1 | [AI 可做]，语义 [共同] |
| 1.2 | 实现统一动作接口与 trace log | 0.2 | [AI 可做] |
| 1.3 | 接通底座，跑通 observe→act→record 单步 | 0.4 | [AI 可做] |
| 1.4 | Agent 执行循环 + 动作 schema 校验 | 1.2、1.3 | [AI 可做] |
| 1.5 | 本地 qwen3:8b 跑通至少 1 个任务到 finish | 1.4、0.3 | [AI 可做] |
| 1.6 | 最小成功判定 + 一份可读 trace | 1.5 | [AI 可做] |

Phase 1 完成标准：有自造任务集、有动作接口与 trace、本地模型能端到端完成至少 1 个任务并产出可复盘轨迹。

### Phase 2：完整能力主体

| Step | 任务 | 依赖 | 负责人 |
|---|---|---|---|
| 2.1 | 任务集扩到 15-20 个，覆盖多页流转/表单/筛选/下载 | 1.1 | [AI 可做]，抽查 [用户-only] |
| 2.2 | planner + executor 拆分（对照单步决策） | 1.4 | [AI 可做] |
| 2.3 | 安全白名单 + 高风险动作拒绝/人工确认 | 0.5、1.4 | [共同] |
| 2.4 | Prompt injection 测试集与防护校验 | 2.3 | [AI 可做]，语义 [共同] |
| 2.5 | 失败恢复（元素缺失/跳转/校验失败时重试回退） | 1.4 | [AI 可做] |
| 2.6 | API 强模型对照基线接入 | 1.5 | [AI 可做]，key [用户-only] |
| 2.7 | 多次运行收集成功率分布（≥3 次/任务） | 2.1-2.6 | [AI 可做] |

Phase 2 完成标准：15-20 任务可跑；有安全边界与 injection 测试结果；有失败恢复；有 local vs API 两套结果。

### Phase 3：评估、交付与收尾

| Step | 任务 | 依赖 | 负责人 |
|---|---|---|---|
| 3.1 | 指标汇总（成功率/步骤数/耗时/无效动作率/恢复率/安全违规率） | 2.7 | [AI 可做] |
| 3.2 | 错误分层（7 类）+ 逐任务 trace replay | 2.7 | [AI 可做] |
| 3.3 | local vs API 显著性（配对 McNemar；成功率 bootstrap CI） | 2.7 | [AI 可做] |
| 3.4 | Streamlit/HTML dashboard（任务/轨迹/截图或DOM摘要/指标） | 3.1 | [AI 可做] |
| 3.5 | README（本地运行、任务集、安全策略、指标、失败案例） | 3.1-3.3 | [AI 可做] |
| 3.6 | 简历中英 bullet（聚焦 agent 执行系统/MCP/可审计/安全） | 真实结果完成 | [共同] |

Phase 3 完成标准：完整指标表 + 错误分层 + 统计对照 + 可演示 dashboard + README + 简历 bullet。

## 4. 你必须亲自完成/确认的事项

1. **是否接入任何真实网站**：默认不接；若接，必须你明确同意并承担登录/反爬/合规后果。
2. **安全策略边界**：哪些动作必须禁止、哪些必须人工确认（支付/删除/发送/提交敏感表单）。
3. **释放 GPU 资源**：跑本地模型前关闭占显存程序。
4. **API key**：如做 local-vs-API 对照，由你提供 key。
5. **最终简历措辞**：必须基于真实跑过的结果，且与 `D:\Jobs\Resume` 现有项目不重复表述。

## 5. 我可以直接完成的事项

搭 repo 结构 / 写依赖 / 自造任务页与 ground truth / 实现动作接口与 trace / 接通底座 / 写 agent 执行循环与 schema 校验 / 写安全白名单与 injection 测试 / 写失败恢复 / 写评估与统计脚本 / 写 dashboard、README、实验记录模板与可复现命令。

## 6. 推荐并行 Conversations

建议开 5 个对话，每个只负责一个工作流，避免踩文件。

### Conversation A：任务环境与脚手架
```text
你负责 D:\Study\Projects\p3 的"本地 Browser Agent / MCP 工作流"项目的任务环境与脚手架。
请先读 落地计划.md 和 候选E_本地浏览器智能体MCP工作流_实施计划.md，只处理项目结构、依赖、Playwright 安装、自造离线任务页与 ground truth。
任务域用通用办公/数据流转，刻意避开金融。不要写 agent 决策或评估逻辑。
```

### Conversation B：浏览器工具层
```text
你负责 D:\Study\Projects\p3 的浏览器工具层。
请读 落地计划.md 与 候选E 实施计划，只处理动作接口（observe/click/type/select/extract/download/finish）、底座接通（Playwright MCP 或 browser-use）、trace log。
不要写模型 prompt 或安全策略语义，发现接口不够用只提变更建议。
```

### Conversation C：Agent 执行闭环与模型
```text
你负责 D:\Study\Projects\p3 的 agent 执行闭环与模型接入。
请读 落地计划.md 与 候选E 实施计划，只处理执行循环、动作 JSON schema 与程序侧校验、provider 切换（本地 qwen3:8b 与 API）。
先让本地小模型跑通单任务，再接 API 对照。不要改任务页内容或评估脚本。
```

### Conversation D：安全边界与失败恢复
```text
你负责 D:\Study\Projects\p3 的安全边界与失败恢复。
请读 落地计划.md 与 候选E 实施计划，只处理动作白名单、prompt injection 测试集、高风险动作拒绝/人工确认、重试/回退恢复。
网页内容一律当作不可信观察；系统规则与白名单由程序侧硬校验。不要改底座动作接口实现。
```

### Conversation E：评估、统计与交付
```text
你负责 D:\Study\Projects\p3 的评估、统计与交付。
请读 落地计划.md 与 候选E 实施计划，只处理指标汇总、7 类错误分层、trace replay、local-vs-API 配对 McNemar 与成功率 bootstrap CI、Streamlit dashboard、README。
评估必须用固定离线任务集与 ground truth；区分成功率、安全违规率、恢复率。不要改任务页或底座。
```

## 7. 并行执行时的文件边界

| Conversation | 主要可编辑路径 | 避免编辑 |
|---|---|---|
| A 任务环境 | `tasks/`、`scripts/serve_tasks.py`、`requirements.txt`、README 环境部分 | agent/eval 逻辑 |
| B 工具层 | `src/browser/`（动作接口、trace） | 任务页、prompt |
| C 执行闭环 | `src/agent/`、`src/providers/`、`scripts/run_agent.py` | 任务页、底座动作实现 |
| D 安全恢复 | `src/safety/`、`tasks/injection/`、`src/agent/recovery.py` | 底座接口 |
| E 评估交付 | `src/eval/`、`src/stats/`、`scripts/evaluate.py`、`app_dashboard.py` | 任务页、底座 |

## 8. 第一周建议排程

| Day | 用户 | AI / 并行对话 |
|---|---|---|
| Day 1 | 确认任务域与安全策略草案 | A 建环境与 3-5 任务页；B 起草动作接口 |
| Day 2 | 抽查任务 ground truth | B 接通底座 + trace；C 执行闭环 skeleton |
| Day 3 | 释放 GPU | C 本地 qwen3:8b 跑通单任务 |
| Day 4 | 审 injection 测试语义 | A 扩到 15-20 任务；D 安全白名单 + injection 集 |
| Day 5 | 提供 API key（可选） | C 接 API 对照；D 失败恢复 |
| Day 6 | 看失败案例 | E 指标 + 错误分层；多次运行收集成功率 |
| Day 7 | 决定 stretch 取舍 | E 统计对照 + dashboard + README 初稿 |

## 9. 立即可执行的下一步

1. 确认本落地计划的方向与安全策略草案（[用户-only]）。
2. 开 Conversation A：建 `D:\Study\Projects\p3` 结构、复用 `.venv`、装 Playwright、自造前 3-5 个任务页，并立刻创建 `实验记录.md`。
3. 开 Conversation B：起草统一动作接口与 trace log，准备接底座。
4. 先用本地 mock 任务页把"观察→动作→记录→成功判定"闭环跑通，再谈扩任务量、安全测试与 local-vs-API 对照。

> 不要急着接真实网站或追高成功率。先让任务集、动作接口、trace、成功判定、安全白名单全部跑通，agent 能力与对照实验才不会变成黑箱试错。
